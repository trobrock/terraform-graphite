apiVersion: 1

datasources:
  - name: Graphite
    type: graphite
    isDefault: true
    access: direct
    url: ${graphite_url}
    jsonData:
      graphiteVersion: "1.1"
    version: 1
    editable: true
  %{ if enable_cloudwatch }
  - name: Cloudwatch
    type: cloudwatch
    jsonData:
      authType: credentials
      defaultRegion: ${region}
    version: 1
    editable: true
  %{ endif }
